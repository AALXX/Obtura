export interface TeamData {
    id: string
    name: string
    updated_at: string
    memberCount: number
    is_active: boolean
}

export interface TeamCard {
    id: string
    name: string
    description: string
}
